import test from 'node:test';
import assert from 'node:assert/strict';
import {evaluateCatalog, evaluateCatalogCompatibility} from './harness.mjs';

test('compatibility harness reports ready browser integrations and configuration separately', () => { const entry = {id: 'cloud', backendType: 'provider', capabilities: ['gamepad', 'webrtc'], requirements: ['provider-account']}; const ready = evaluateCatalogCompatibility({...entry, requirements: []}, {input: {gamepad: true}, transports: {webrtc: true}}); assert.equal(ready.status, 'ready'); const setup = evaluateCatalogCompatibility(entry, {input: {gamepad: true}, transports: {webrtc: true}}); assert.equal(setup.status, 'configuration-required'); assert.deepEqual(setup.configuration, ['provider-account']); });
test('compatibility harness fails closed for missing capabilities and native emulators', () => { const missing = evaluateCatalogCompatibility({id: 'cloud', backendType: 'provider', capabilities: ['hardware-decode'], requirements: []}, {media: {hardwareDecode: {h264: {powerEfficient: false}}}}); assert.equal(missing.status, 'browser-capability-missing'); const native = evaluateCatalogCompatibility({id: 'dolphin', backendType: 'emulator', mode: 'native', capabilities: [], requirements: []}, {}); assert.equal(native.status, 'native-adapter-required'); });
test('catalog evaluation provides aggregate status counts', () => { const result = evaluateCatalog([{id: 'a', backendType: 'provider', capabilities: [], requirements: []}, {id: 'b', backendType: 'emulator', mode: 'native', capabilities: [], requirements: []}], {}); assert.equal(result.counts.ready, 1); assert.equal(result.counts['native-adapter-required'], 1); });
