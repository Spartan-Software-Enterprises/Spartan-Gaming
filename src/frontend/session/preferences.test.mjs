import assert from 'node:assert/strict';
import test from 'node:test';
import {createSessionPreferences} from './preferences.mjs';

test('session preferences map streaming settings into bounded capabilities', () => {
  const result = createSessionPreferences({'streaming.resolution': '1440p', 'streaming.framerate': '120 FPS', 'streaming.codec': 'VP9', 'streaming.bitrate': 40, 'streaming.qualityPreset': 'Prefer latency', 'media.hdr': true, 'controllers.allowGamepad': false});
  assert.deepEqual(result.capabilities.video.codecs, ['vp9', 'h264']);
  assert.equal(result.capabilities.video.maxWidth, 2560);
  assert.equal(result.capabilities.video.maxFramerate, 120);
  assert.equal(result.capabilities.video.hdr, true);
  assert.equal(result.capabilities.input.gamepad, false);
  assert.equal(result.preferences.qualityPreset, 'low');
  assert.equal(result.preferences.bitrateKbps, 40000);
  assert.equal(result.preferences.qualityProfiles.find(profile => profile.id === 'high').maxWidth, 2560);
  assert.equal(result.preferences.autoFullscreen, true);
  assert.equal(result.preferences.pictureInPicture, true);
  assert.equal(result.preferences.showOverlay, true);
});

test('session preferences fall back safely for invalid settings', () => {
  const result = createSessionPreferences({'streaming.bitrate': 'not-a-number', 'streaming.framerate': 'bad', 'streaming.codec': 'unknown'});
  assert.equal(result.capabilities.video.maxWidth, 1920);
  assert.equal(result.capabilities.video.maxFramerate, 60);
  assert.deepEqual(result.capabilities.video.codecs, ['av1', 'vp9', 'h264']);
  assert.equal(result.preferences.bitrateKbps, 25000);
  assert.equal(result.preferences.qualityProfiles.find(profile => profile.id === 'ultra').maxWidth, 1920);
  assert.equal(result.preferences.autoFullscreen, true);
});
test('session preferences carry bounded display selection and refresh policy', () => { const result = createSessionPreferences({'media.display': 'Display 2', 'media.refreshRate': '240 Hz'}); assert.deepEqual(result.preferences.display, {kind: 'index', index: 1}); assert.equal(result.preferences.maxRefreshRate, 240); const ask = createSessionPreferences({'media.display': 'Ask each time', 'media.refreshRate': 'Automatic'}); assert.equal(ask.preferences.display, 'ask'); assert.equal(ask.preferences.maxRefreshRate, null); });
test('session preferences carry the touch-control layout choice', () => { assert.equal(createSessionPreferences({'accessibility.touchLayout': 'Minimal'}).preferences.touchLayout, 'Minimal'); });
test('session preferences honor the Picture-in-Picture setting', () => { assert.equal(createSessionPreferences({'gaming.pictureInPicture': false}).preferences.pictureInPicture, false); });
test('session preferences carry telemetry visibility and bounded game volume', () => { const result = createSessionPreferences({'streaming.showTelemetry': true, 'media.gameVolume': 35}); assert.equal(result.preferences.showTelemetry, true); assert.equal(result.preferences.gameVolume, 0.35); assert.equal(createSessionPreferences({'media.gameVolume': 150}).preferences.gameVolume, 1); });
test('session preferences carry audio mix and processing policy', () => { const result = createSessionPreferences({'media.gameVolume': 35, 'media.chatVolume': 55, 'media.spatialAudio': true, 'accessibility.monoAudio': true, 'media.audioInput': 'No microphone', 'media.micNoiseSuppression': false}); assert.equal(result.preferences.gameVolume, 0.35); assert.equal(result.preferences.chatVolume, 0.55); assert.equal(result.preferences.spatialAudio, true); assert.equal(result.preferences.monoAudio, true); assert.equal(result.preferences.audioInput, 'No microphone'); assert.equal(result.preferences.microphoneNoiseSuppression, false); assert.deepEqual(result.capabilities.audio, {codecs: ['opus', 'aac'], channels: 2, spatialAudio: true, mono: true}); });
test('session preferences prioritize a supported preferred codec while preserving fallback codecs', () => { const result = createSessionPreferences({'streaming.codec': 'VP9'}); assert.equal(result.preferences.preferredCodec, 'vp9'); assert.deepEqual(result.capabilities.video.codecs, ['vp9', 'h264']); const unsupported = createSessionPreferences({'streaming.codec': 'AV1'}, {media: {codecs: {av1: false, vp9: true, h264: true}}}); assert.equal(unsupported.preferences.preferredCodec, 'av1'); assert.deepEqual(unsupported.capabilities.video.codecs, ['vp9', 'h264']); });
test('session preferences carry validated recording and audio output choices', () => { const result = createSessionPreferences({'media.recordingCodec': 'H.264', 'media.recordingLocation': 'Ask each time', 'media.audioOutput': 'Headphones'}); assert.equal(result.preferences.recordingCodec, 'H.264'); assert.equal(result.preferences.recordingLocation, 'Ask each time'); assert.equal(result.preferences.audioOutput, 'Headphones'); const fallback = createSessionPreferences({'media.recordingCodec': 'unsupported', 'media.recordingLocation': 'unknown', 'media.audioOutput': 'unknown'}); assert.equal(fallback.preferences.recordingCodec, 'Automatic'); assert.equal(fallback.preferences.recordingLocation, 'Videos/Spartan Gaming'); assert.equal(fallback.preferences.audioOutput, 'System default'); });
test('session preferences carry controller layout and bounded dead-zone settings', () => { const result = createSessionPreferences({'controllers.defaultProfile': 'PlayStation layout', 'controllers.deadzone': 20}); assert.equal(result.preferences.controllerProfile, 'PlayStation layout'); assert.equal(result.preferences.controllerDeadzone, 0.2); assert.equal(result.preferences.controllerBindings.confirm, 'button-1'); assert.equal(createSessionPreferences({'controllers.deadzone': 100}).preferences.controllerDeadzone, 0.3); });
test('session preferences negotiate explicit HID and advanced controller capabilities', () => { const result = createSessionPreferences({'controllers.allowHid': true, 'controllers.adaptiveTriggers': true, 'controllers.gyro': true, 'controllers.inputLatency': 'High frequency'}); assert.deepEqual(result.capabilities.input, {gamepad: true, keyboard: true, pointer: true, rumble: true, hid: true, adaptiveTriggers: true, gyro: true}); assert.equal(result.preferences.inputPolling, 'High frequency'); const fallback = createSessionPreferences({'controllers.inputLatency': 'unsupported'}); assert.equal(fallback.preferences.inputPolling, 'Automatic'); assert.equal(fallback.capabilities.input.hid, false); });
test('session preferences carry the bounded instant replay policy', () => { const result = createSessionPreferences({'gaming.instantReplay': true, 'gaming.replayLength': '120 seconds'}); assert.equal(result.preferences.instantReplay, true); assert.equal(result.preferences.replayLengthSeconds, 120); assert.equal(createSessionPreferences({'gaming.replayLength': 'invalid'}).preferences.replayLengthSeconds, 30); });
test('session preferences carry the focus pause policy', () => { assert.equal(createSessionPreferences({'gaming.pauseOnBlur': true}).preferences.pauseOnBlur, true); assert.equal(createSessionPreferences().preferences.pauseOnBlur, false); });
test('session preferences inherit active workspace quality, controller, and overlay defaults', () => { const result = createSessionPreferences({'streaming.qualityPreset': 'Balanced', 'controllers.defaultProfile': 'Xbox layout', 'gaming.showOverlay': true}, undefined, {id: 'guest', quality: 'low', controllerProfile: 'keyboard', overlay: true}); assert.equal(result.preferences.qualityPreset, 'low'); assert.equal(result.preferences.controllerProfile, 'Keyboard and mouse'); assert.equal(result.preferences.showOverlay, true); const family = createSessionPreferences({}, undefined, {id: 'family', quality: 'balanced', controllerProfile: 'auto', overlay: false}); assert.equal(family.preferences.showOverlay, false); });
test('session preferences consume a saved custom controller profile selected by workspace', () => { const result = createSessionPreferences({}, undefined, {id: 'arcade', quality: 'balanced', controllerProfile: 'arcade-stick', overlay: true}, [{id: 'arcade-stick', name: 'Arcade stick', deviceMatch: 'any', bindings: {confirm: 'button-2', cancel: 'button-3'}, deadzone: 0.04, rumble: false}]); assert.equal(result.preferences.controllerProfile, 'Arcade stick'); assert.equal(result.preferences.controllerBindings.confirm, 'button-2'); assert.equal(result.preferences.controllerDeadzone, 0.04); assert.equal(result.capabilities.input.rumble, false); });
test('session preferences carry the privacy clear-on-exit policy', () => { assert.equal(createSessionPreferences({'privacy.clearOnExit': true}).preferences.clearOnExit, true); assert.equal(createSessionPreferences().preferences.clearOnExit, false); });
test('session preferences gate local telemetry collection through privacy settings', () => { assert.equal(createSessionPreferences().preferences.sessionTelemetry, true); assert.equal(createSessionPreferences({'privacy.sessionTelemetry': false}).preferences.sessionTelemetry, false); });
test('session preferences gate refresh recovery through general settings', () => { assert.equal(createSessionPreferences().preferences.restoreSession, true); assert.equal(createSessionPreferences({'general.restoreSession': false}).preferences.restoreSession, false); });
test('session preferences apply observed display policy evidence without storing capabilities', () => { const result = createSessionPreferences({'media.hdr': true, 'media.refreshRate': '240 Hz', 'streaming.codec': 'Automatic'}, {graphics: {hdr: false}, display: {maxRefreshRate: 144}, media: {codecs: {av1: false, vp9: true, h264: true}}}); assert.equal(result.capabilities.video.hdr, false); assert.equal(result.capabilities.video.maxFramerate, 144); assert.deepEqual(result.capabilities.video.codecs, ['vp9', 'h264']); assert.equal(result.preferences.displayPolicy.maxRefreshRate, 144); });
