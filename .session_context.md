# Spartan Gaming Session Context

Updated: 2026-08-15

The canonical checkout is `/home/ubuntu/Spartan-Gaming` on the KVM; GitHub is the source of truth.

## Current release

- `main` HEAD: `318dfac`
- Latest release: `v0.1.0-beta.6` (GitHub prerelease)
- Release URL: https://github.com/Spartan-Software-Enterprises/Spartan-Gaming/releases/tag/v0.1.0-beta.6
- Assets: signed Android APK and portable `SHA256SUMS.txt`
- Tag and commit are GPG-signed with the registered Codex key.
- Worktree is clean and `main` tracks `origin/main`.

## Current implementation

The frontend has live device-aware viewport sizing, mobile/handheld compact behavior, safe-area spacing, responsive dialogs/cards/controls, resize tracking, and the tested settings scroll reset. Screen orientation behavior remains unchanged.

Beta.6 adds Kotlin formatting support. `npm run format:kotlin` writes Android `.kt` and `.kts` files with `com.facebook:ktfmt:0.64`; `npm run format:kotlin:check` validates them. `npm run format` and `npm run format:check` run both. The old `prettier-plugin-kotlin` experiment failed on this repository's real syntax and is not installed.

## Connection instructions for OpenCode

- OpenCode loads `/home/userland/AGENTS.md` through `/home/userland/.config/opencode/opencode.jsonc`.
- Use the configured `kvm-spartancode` SSH alias and existing `SpartanDev` agent for KVM/GitHub access.
- Use the existing `Codex` GPG configuration for signed commits/tags.
- Use the authenticated `gh` session for GitHub checks/releases.
- Use the authenticated `google-drive` rclone remote for Android-safe exchange.
- Android SDK: `/home/ubuntu/android-sdk` on KVM.
- Signing and rclone configuration paths are protected; use them through existing environment/configuration and never print their contents.

No tokens, passwords, private keys, keystores, cookies, or raw credential stores belong in this file.

## Verified commands

- `npm test`: 815 passed, 5 skipped, 0 failed out of 820.
- `npm run electron:test`: 57/57 passed.
- `npm run format:check`: passed.
- `npm run check`: passed.
- `npm run test:android-shell`: 5/5 passed.
- Android signed release build and APK v2 signature verification: passed.
- Beta.6 APK download and checksum verification: passed.
- Playwright smoke: 11 routes at desktop/mobile.
- Supplemental UI matrix: 14 routes x 2 viewports, 28 screenshots, no browser/page errors or horizontal overflow.

## Next-agent rules

Check `git status -sb`, `git log -1`, and the GitHub release before changing anything. Work from the KVM checkout, preserve a clean tree, keep temporary QA files outside the repository, and never publish another beta without explicit instruction.
